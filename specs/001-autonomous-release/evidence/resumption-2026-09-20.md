# 2026-09-20 resumption evidence

## Task46 accepted — combined scoped evidence

Controller accepted after fully reading the clean nine-path and one-file independent reviews,
rehashing all807 final paths at db384ceb994654c40ae0388d3358e18f7edff90ee01197ce99f514fa9adf9318,
and reconciling all96 original failed/error nodes against latest passing cover. Final source
scope34new/32modified=66,741 outside unchanged. All15 retained normative/research/amendment
snapshots unchanged; HEAD a2f85d578c47a0e59c1850ac1840cf8baceb0d96 and no commits/push.

Final22 was548pass/1fail; only one test file then changed (C6/V6/comment), and whole API→FD
cover122passed/1warning63.94s with806other paths identical. All549 covered node identities have
passing latest evidence across those runs. Neither failed22 nor failed72 is relabeled green.
Ruling: accept this finite combined proof; retain later integration/whole-branch/native gates
for possible interactions. Unchanged pure/capacity evidence is retained, not newly executed.

ColdHTTP GET assertions cover status/nullable receipt fields, NOT full raw response bytes;
POST/cancel are byte-exact. This corrects broader wording in earlier briefs/writer chronology.
The original final-wall2-of6 cause remains UNKNOWN despite current combined pass. Carry that
recurrent historical uncertainty to integrated/native-fit/final review; no production activation
or real IPC latency/reliability claim follows. One inherited Starlette warning remains.
Detailed final adjudication: scratch task-46-acceptance.md, task-46-combined-final-evidence.json,
task-46-cold-http-review.md and exact before/after manifests. Next semantic design's accepted46
gate is now satisfied; scope reconciliation/brief still precede implementation dispatch.

### Retained regression chronology (earlier pending states superseded above)

The frozen R1 exact72 finished93 failed/2834 passed/3 errors, exit1 in3053.55s.
All96 outcomes are reconciled in scratch task-46-regression-diagnosis.md. A real unconditional
writer check broke83 legacy/read-only outcomes; one conditional guard now distinguishes empty/
legacy inspection from retained verification/view issuance. Genuine RED2failed/2passed became
GREEN4passed on explicit authority-boundary tests; representative21 and correction23 passed.
Other changes are test expectations for closed schema arms, current V6 and the third source
export, genuine held-V2 fixture composition, per-case FD closure measurement, and final-wall
failure metadata. The original worker2-of6 cause is unknown, not cured by its diagnostic pass.

Controller authorized exact9 repair paths against verified R1 beforecopies, adding only four
test files to original62 ownership: provider_conformance_contracts, deployment_prepare_integrity,
deployment_receipt_api and provider_conformance_lifecycle (all under app/tests/test_*.py).
This gives66 total scoped paths/807 inventory; no archived normative snapshot changes. Remaining
source/test bytes stay held. Scope and before hashes: scratch task-46-regression-scope.json.
The one finite same-process final22 completed548passed/1failed/1warning1612.22s, exit1
(session2303 reaped); all549 unique outcomes parsed and95/96 original failed/error nodes now
pass. Both finalFD0/64 cases passed after all families in the SAME process; final-wall also
passed without a clock/timeout change. Original cause remains unknown. All807postrun hashes
remain e451621e1dc53062c6643857d3d64c068dfaaae27761d57e225c5670d438162d; diffcheck clean.
Sole remaining coldV1HTTP failure is a now-unmasked current-success C5 expectation with actual
reviewedC6; following shape assertion also staleV5. Its real historical snapshot/login pass;
remaining HTTP byte checks await correction. Samewriter authorized one-file test-only change
and whole API→FD family cover; all product bytes held. No automatic full-suite retry, limit
relaxation, acceptance or new all-green22/72 claim. Independent9delta spec/quality review clean.

Worktree `codex/ui-structure`, unchanged HEAD
`a2f85d578c47a0e59c1850ac1840cf8baceb0d96`. Changes remain uncommitted; no publication,
live/paid provider requests, native deployment or user-store operation was performed.

## Task45 — accepted scoped private-provider conformance

The fixed private-worker suite now has actual framed execution, independent literal comparison,
bounded retained capture, same-store intent/report history, authenticated owner routes and
source-less restart/replay. It is operational conformance evidence, not DeepTwin growth
evaluation, real-model qualification or an enabled provider binding.

Final source inventory:773 paths; SHA256
`d6cc1b4460d37b4cc0789c52e6e90b9a002c931f6796c8a51fa251c364b89ba6`.
Controller independently verified all entries after review. R5 changed exactly four test/fixture
paths from the production-reviewed R4 snapshot; all769 other paths, including production, stayed
byte-identical. Task scope is original34 paths plus two expressly admitted compatibility tests.

| Retained run | Result | Evidence meaning |
| --- | --- | --- |
| R4 exact72 modules, session90316 |13 failed /2915 passed /1 warning; exit1;3137.65s|Completed RED baseline; never relabeled GREEN. |
| Unchanged13 affected nodes,62807 |12 failed /1 passed; exit1|Catalog/route assumptions reproduce; wall test passes in isolation. |
| Controlled final-identify delay,50732 |1 failed; exit1|Shows four vector matches can coexist with incomplete final identify. |
| Final R5 affected modules/requester,24438 |166 passed /1 explicitly deselected /1 warning; exit0;400.58s|All13 original failed nodes pass on final bytes. |
| Identical controlled delay after repair,67417 |1 passed; exit0;25.09s|Six real successful worker calls/full capture precede test-local wall jump. |
| Additional HTTP/service fixture compatibility,37061 |5 passed /1 warning; exit0;95.30s|Actual positive service, HTTP and source-less restart paths. |

The sole deselected lifetime-capacity test passed in the completed exact72 run; its function,
staged-app/recovery dependencies and production bytes were unchanged, and it does not use the
changed worker helper. This reuse was explicitly authorized, not an undisclosed skip.

R4 independent review approved the production boundary corrections. R5 independent review found
all three regression causes addressed, zero open/new findings, spec compliant and quality Approved.
The R5 test repair preserves strict worker teardown, proves the complete-input precondition,
selects the actual deployment contribution by identity, and updates independently enumerated route
expectations while retaining failure/ownership assertions.

Controller ruling: accept this combined task-scoped evidence; rerun the connected baseline/final
regression with subsequent integration. There is **no new all-green exact72 run**. The precise
historical worker exception is not recoverable from the retained failure and remains unproved;
the controlled mechanism is not its asserted diagnosis. Deterministic functional clock isolation
does not establish real IPC latency. One inherited Starlette deprecation remains disclosed.

Detailed exact commands/output, before snapshots, reports and reviews are retained in
`.superpowers/sdd/resumption-plan/`, especially `task-45-report-r5-frozen.md`,
`task-45-final72-r4-run.md`, `task-45-r4-full72-failures.txt` and `task-45-r5-review.md`.

## Next boundary and whole-product status

Task46 installation release-evidence/source/history and verified-descendant conformance designs
passed independent preflight after F1–F4 corrections and were promoted. Tagged primary schema/source
text was retained and used for the bounded implementation; no native scanner operation followed.
The exact62-path scope and773 baseline/28beforecopies are preserved. Implementation now has clean
scoped connected R1 review but is **not accepted** until final-byte test results are adjudicated.

Frozen807-path manifest:
`91dfa646c13e8fb80e5728f3e4606738d0729e62363ae0f67a344f8d39ae5919`.
All62 scoped files changed;745 outside-scope files remain unchanged. Controller verified exact
R0 copies and seven R1 deltas. Early A durability and B native/path findings are closed; connected
R0's three P2 findings (new SQL timestamp upper bound, malformed published record JSON, preliminary
history load before association bounds) are fixed and independently closed in R1. No new actionable
finding remains in that review. The review's original228 lines and134-line R1 remain preserved in
`.superpowers/sdd/resumption-plan/task-46-connected-review.md`.

| Execution evidence so far | Result | Boundary |
| --- | --- | --- |
| Intermediate combined11 |422 passed /1 failed /1 warning;913.67s|Malformed record JSON; pre-clock-fix imports. Not final cover. |
| Exact SQL/source/read/replay/migration gap group |6 passed /62 deselected /1 warning;55.87s|Includes genuinely coexisting old-tool/provider/completed-B V5, all rebuild checkpoints and source-less replay. |
| Corrected owned schema parity |11 actual JSON exports equal; selected3 tests passed0.62s|Only the malformed record artifact regenerated losslessly. |
| Legacy/new intent index preflight RED/GREEN |4 failed31.16s →4 passed30.81s|Extra valid indexed edges/blobs refuse before target record materialization. |
| Terminal legacy B to fresh verified B |1 passed47.26s|Two actual six-connection/four-vector runs, old immutable reply preserved past its run deadline; not a B evidence TTL. |
| Final combined11 at R1 bytes |435 passed /1 inherited warning;1042.20s, exit0|All807 postrun hashes unchanged; raw terminal log inspected by controller. |
| Final ordered72 at same R1 bytes |93failed /2834passed /3errors /1 inherited warning;3053.55s, exit1|Completed/reaped25969, all807 hashes unchanged. Regression diagnosis required; not accepted. |

Changed-actor replay proof is explicitly an isolated counterfactual after real bound owner
authentication, not a second-owner workflow. Accepted B has no evidence expiry policy; future C
owns qualification freshness. The graph proof separately covers the literal shared64MiB/+1 guard
and the coherent installation's finite54657024-byte ceiling. These limitations are not omitted.

Final all11 completed once, session73159 reaped. The controller's unchanged ordered exact72 started
sequentially at2026-09-20 06:01:01UTC, session25969, log task-46-final72-r1.log, and completed with
the failed result above. Root verified the same807-path manifest and clean diffcheck afterward.
The same writer is diagnosing the16 failing/error modules before exact repair scope is approved.
No retry, skip or source/test edit has occurred. Original13 installation/
semantic design snapshots were independently rehashed unchanged. The next Claude semantic execution
design is promoted, but implementation still waits for accepted46 and exact ownership reconciliation.
Real release artifacts, trusted inputs,
native qualification and provider binding remain separate gates. Browser input-to-understanding,
real graph generation/critique, typed artifact execution, lens inquiry, actual paired prior-queue
evaluation and human environment promotion still require integration.

Whole-product estimate remains **about50% ±5 including design**; no defensible whole-project ETA.
Passing scoped tests does not close T042/T087 or any uncompleted whole-story requirement.
