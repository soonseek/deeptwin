# 2026-09-19 resumption evidence

Worktree: `codex/ui-structure`, base `a2f85d578c47a0e59c1850ac1840cf8baceb0d96`.
Six inherited dirty files were preserved before implementation. Main checkout unchanged;
no commits, push, paid/live model calls, credential discovery or host deployment.

## Current evidence, not whole-product completion

- 2026-09-20: Task42 accepted after independent review and fixture-only R1. Reached requester
  ownership/cleanup production cover:315passed/2inheritedskips/21subtests/no warnings. The review
  found invalid FD usage and missing post-bind cleanup in a new test fixture;7RED→7GREEN and
  final amended lifecycle203passed/no warnings corrected it. Main verified720 manifest entries,
  one R1 test change/719 unchanged and original report prefix. Supplemental checkpoint tests were
  post-repair GREEN, not historical RED; this procedural limitation remains disclosed.
  Task43 full preflight READY and actual42 dependency diff reconciled. Its49-path connected
  receipt/observer/installation/history/API delivery begins next; native qualification remains open.

- Task41 accepted after independent R1 review: both primary-exception masking and missing
  integration-evidence findings addressed, no new Critical/Important breakage. Actual owner/CAS/
  v4 history/publication/HTTP paths are connected within this scope. Final R1 eight-family run:
  199passed/1inheritedStarlettewarning362.95s. Main verified719 current/report hashes,30 owned
  hashes and the original report prefix. Accepted40 baseline704→719:689 unchanged+15mod+15new.
  Initial32-family63failed/1040passed and test-only ordered421passed are separate preserved runs,
  not a fresh all-green32/full-suite claim. Historical graph tests were post-implementation;
  procedural deviation disclosed. R1 actual double-fault RED→GREEN and all required added proofs
  retain their limits: targeted real-history consumer negatives/internal byte-cap inputs do not
  imply fully rewritten persisted forgeries/legal padded public requests. No native/provider
  execution/receipt/installation/qualification/binding or whole-story success. Task42 requester
  lifetime now active with accepted719 and three exact beforecopies; no existing-test edits.

- Task40 accepted after independent spec compliant/quality Approved; final exact7family
  263passed/1unchangedroot-requiredskip/1inheritedStarlettewarning46.63s.96new lifecyclecases.
  Mainfullyreadreport/review andfreshverified704=700outsideunchanged+3mod+1new; no removals.
  Historical RED/fixture progression retained; skip marker and uid501 independently inspected.
  This was the checkpoint preceding the accepted Task41 result above.

- Task39 accepted, independent spec compliant/quality Approved, no blockers. Exactfrozen6family
  506passed33.33s/oneinheritedStarlettewarning; measuredtotalpeak122inclcallerbaseline; main
  freshverified703paths=697outsideunchanged+3authorizedmod+3new. Simulation/native boundaries
  explicit. Task40 solewriter /root/task40_slot_lifetime_worker Astra/high started with703baseline,
  threecmpverifiedbeforecopies/newtestabsent. Tasks41/42queuedpreflightREADY.

- Task38 accepted after R2 independent spec/quality Approved; allthreeImportant and importminor
  closed. R1fivefamily366passed4.33s, R2test-only68passed2.98s, no warnings. Mainverified700hashes/
  690originaloutsidepreserved; R2onefilechanged/699same. Initialfailedtestfixtures and tracing
  omission remain recorded below/report, not erased. Generic DomainStore walker unchanged.
  Task39 solewriter /root/task39_provider_publication_worker Astra/high started with700baseline,
  threecmpverifiedbeforecopies and3newpathsabsent. Task40/41preflightREADY; notyetimplemented.

- Task38 independent review: spec issues/quality Needs fixes, three Important findings (pure
  inventory cap, required negative cases, oversize error classification). Original writer R1 is
  active within the same ten paths. Corrective verification of the unchanged initial700-file freeze
  with both tracing flags false passed328tests3.56s/no warnings; this is not R1 acceptance.
  Controller's pre-acceptance anchor field correction preserves all18 named refs without changing
  the generic DomainStore walker. Task41 now has a normative27-path contract; three preflight
  blockers are under scoped correction review, not implementation or admission approval.

- Task37 accepted: independent spec compliant/quality Approved; minor inheritedStarlette/anyio
  deprecation retained for laterdependencyreview. Frozen nine-family549passed79.09s; controller
  verifiedall690hashes afterreview,3authorizedmod+4new/683outsideunchanged. Actual controlled
  startupowner and closing-snapshot regression covered; observedpeak113FD inclscandir/111retained.
  No currenthandle admission/native/liveclaim. Task38 tennewpureprotocolfiles active;39preflightREADY,
  Task40slot-lifetime preflightREADY; integrated v4/producer subsequently promoted to normative
  contract under independent preflight, not implementation. Maincheckoutclean at this checkpoint.

- Task36 accepted, independent spec compliant/quality Approved with no findings: exact6family506passed27.90s
  once afterfreeze, no warnings; all8hashes and whole686manifest match freshfiles. Baseline678outside
  paths unchanged;2authorizedexistingchanges+6new. PeaktrackedownedFD91/final0; staged/final
  overlap19+19with89live. Earlier-leaf mutation race and unexplained new-namespace payload adoption
  both reproduced RED then fixed GREEN. Report preserves other fixture/lint failures. No native,
  real provisioning, source runtime admission, live model or whole-story completion claim.
  Task38 doc preflight nowREADY: excludes same candidate identity from preserved inventory and
  pins head_digest to exact existingstorage-v3 installation_heads hash, not its anchor digest.

- Task36 intermediate checkpoint, not acceptance: independent ordinary lifecycle characterization
 12passed before existing helper edits; interruption RED87failed/15passed then102passed;
 namespace cleanup RED39failures then corrected; lifecycle plus unchanged file/prepare/receipt
 initializer coverage259passed4.94s. New initializer fixture executes actual acceptedTask35
 renderer/validator and first2RED reflect this task's absent implementation. Shared observer and
 complete initializer are still active; final frozen coverage/independent review have not run.
 Task37 preflight READY only; Task38 protocol contract under preflight, not implemented/migrated.

- Task35 accepted: independent spec/quality Approved with no findings; exactsix-family frozen
  cover246passed2.81s/no warnings. Controller freshly verified680sourcepaths=661unchanged+19new,
  all19reportedhashes. Actual independent18-file expected-byte fixtures, deferred-source-edge
  rejection with refreshed outerpins, finite-float-preserving additiveCompose and blocked-effect
  pure execution pass. Newrecipe867canonicalbytes; no initializedhost/builtimage/currentness/
  qualification claim. Task36 consumes the generated exact bundle separately.

- Task34 accepted: independent spec/quality Approved with no findings;297passed1.83s on exact
  frozenfive-family cover, no warnings/skips. Controller verified661sourcepaths=657unchanged plus
  fournewfiles and allfourreportedhashes. Pure two-platform provider lineage/schema/descriptor/
  provenance/argv joins are implemented; declared images/extraction hashes remain unauthenticated
  structural values, never deployment, qualification, slot or execution authority.

- Task33 accepted after independent full review and scoped R1 rereview. Initial frozen19-family
  run756passed41.54s/no warnings/skips; R1 affected five-family run243passed2.28s/no warnings/skips.
  Real framed regressions first reproduced a5000digit SSE integer preventing final publication
  and secondary connection-close errors masking primary interruptions. Both now corrected;
  prior nullable usage and fresh metadata survive parse failure, deadline/integrity exceptions
  propagate, and cleanup preserves the primary interruption while closing owned resources.
  Main verified657final source entries, only2R1changes/655unchanged and20owned final hashes.
  Local controlled fixtures do not prove Linux isolation, qualified OCI images, canonical
  provider/catalog/model integration or paid/live sends. Full reports/reviews in SDD ledger.

- Fresh pre-fix audit: 223 Python tests passed, one inherited Starlette warning, 77.72s.
  Graph contract/generation, extension transport, scheduler attempts, works API, web assets,
  growth-loop and promotion-approval families. Six shell Node modules: 58 passed, 313ms.
  Node tests use injected DOM/fetch; they are not browser E2E evidence.
- Canonical tasks: 40/90 checked at handback. Approximate product progress: about50%±5
  including design, not a ratio derived from test counts. Whole-project ETA unsupported.
- At initial audit, the supported web work page stored text but not files. AcceptedTask29 below
  now connects original upload/download/recovery; STT/provider/understanding/design generation
  remain unconnected there. Current run panel is a list, not final graph UX.
- Persistent extension admission reaches staged installation; actual durable qualification/
  enabled binding and a canonical tool-definition loader still need implementation.
  Staging, constructor-issued values and a positive probe are not qualification.

## Task26 connection lifetime correction — accepted

Changed only transport lifetime and four real-connection regressions. Ordinary intent-record
failure and BaseException now close the acquired authenticated connection; exchange/success
controls close it once too. Cleanup OSError does not mask original result/failure. Tests track
the real connection and owned socket, with zero execute frames on intent failure.

RED: 2 failed/2 controls passed. GREEN: 4 passed. Parent fresh four-case check: 4 passed,
43 deselected, one inherited warning, 2.95s. Scoped Ruff and diff checks passed.

First frozen covering run: **112 passed/1 failure,58.80s**. Failure was
`test_the_approval_is_looked_up_under_the_gate_that_supplies_it`, `checkpoint journal halted`.
Isolated and two focused adjacency probes passed but did not establish the original cause.
One authorized complete covering run on identical frozen bytes then passed **113 tests**, one
warning,59.18s. The original failure is **unresolved**, not relabeled inherited/fixed/harmless.
No retry loop, source change or silent suppression was used to obtain that evidence.

Independent review found no changed-line defect and requested clean complete covering evidence.
Scoped evidence re-review accepted that requirement after the113-test run. Final two-file
hashes and610out-of-scope app/deploy/schema files were independently verified unchanged.
Detailed before copies, reports and reviews are in `.superpowers/sdd/resumption-plan/`.

## Separate deterministic checkpoint timestamp defect

The actual installed `langgraph.pregel._checkpoint` producer uses `datetime.isoformat()`.
A temporary real StateGraph/saver/store with its producer clock fixed to microsecond0 emitted
`2026-09-19T00:00:00+00:00`; the saver rejected it with `Unsupported checkpoint profile`.
The otherwise identical123456-microsecond control passed and produced counter1. This confirms
a separate compatibility defect, **not the cause of the earlier unidentified failure**.
Task28 now normalizes legitimate whole-second ingress into the existing six-digit journal
format, preserving persisted grammar and historical bytes. Actual-producer RED was one failure
with one passing nonzero control; the frozen four-file cover passed 163 tests and the parent's
actual-producer/year-bound check passed four. Independent spec/quality review accepted it;
two final hashes and 611 outside files were verified. The inherited warning remains visible.

## Current implementation

Task27 connects exact compiler graph/node/binding/definition/grant/tool/effect facts to actual
read-tool dispatch, requires same-ledger ToolCall tracking and contains the inadequate legacy
external approval path. It is accepted within that scope. Canonical qualification, exact
per-call approvals and durable parent/child tool continuations remain required afterward.
The corrected initial covering run passed 245 tests (one Starlette deprecation warning);
the parent's separate focused run passed 33 tests. Independent review then reproduced a
direct-call issuer gap: a second RuntimeLedger over the same DomainStore could accept the
first ledger's consumed window. Matching persisted rows did not establish exact issuer
identity. Fix round 1 adds ledger-owned process-local window validation; independent rereview
accepted I1 as addressed with no new Critical/Important breakage. The amended seven-file cover
passed 249 tests, and the parent's fresh direct-call/revocation check passed seven. Ten final
hashes and 603 outside baseline artifacts were verified. Existing warning and 19 baseline lint
diagnostics remain visible. No persistent schema or external tool permission was widened.

Task29's originals-only intake contract now has a bounded design-review PASS after fixing
replay-before-revision ordering and exact pending-command recovery across refresh. It includes
file-first creation without an invented explanation, retained originals on text edits and
truthful stored-versus-read states. This is a reviewed design, not implemented upload support.
Implementation follows accepted Task28. Its initial freeze passed246Python/40Node/3real-owner
browser tests and parent6actualbackend regressions; all28hashes and595outside files verified.
Independent review then reproduced a browser recovery defect: receipt404→reselectoriginal→
late committed receipt leaves the selection queued, causing a fresh duplicate upload command.
Fix round1 added4genuineREDcases and correctedexactmatching receiptconsumption. Final44Node
and3actualownerbrowser pass; parent4race tests pass. Independent scopedrereview acceptsI1
withnonewbreakage.28finalhashesverified; onlyworkcontroller/testchangedfrominitialfreeze.
Task29 isacceptedwithinoriginalsstoragescope. Sixfinalviewport/theme
screenshots were visually checked, including top-positioned materials action and aligned layout.

Task30 encrypted-custody correction is accepted after R1. Synthetic-only diagnosis of the original
backend proved plaintext equality and a plaintext-derived digest on disk. Actual replacement
provides authenticated encrypted storage, exact v2 reconciliation, retained nonce/retirement
history and bounded credential-local fragmentation without widening broker limits. Initial
eight-family freeze passed161tests/1Starlettewarning19.10s; parent9focusedchecks passed1.41s.
Independentreview found retainedstagefalse-loss and recoverymutationbeforeglobalcorruption.
R1 added10genuineRED/2controls, then12GREEN; full changed3-family87pass10.14s/no warnings and
parent7recoveryregressions0.28s. Independent scopedreview acceptedboth withno newbreakage.
All17finalhashesverified;632-filemanifest R1delta exactly3files,629unchanged. Original615outside
baselinefiles preserved. Reports/exactdiffs/beforecopies retained under.superpowers/sdd/resumption-plan.
NativeUID/mount/peer isolation and stdlibSQLiteVFS sameUIDpath race remain unqualified. Restored
ciphertext does not activate binding or reverse retirement. No actualkey enrollment, automatic
legacy migration, rotation/erasure, live send, realdata operation or commit was performed.

Task31 shared legacy dispatch-context refactor is accepted. Four characterization tests passed
before production edits; eight missing-interface RED cases then passed. Frozen scoped cover:
337 passed/1 inherited Starlette warning in191.53s; parent3focused checks passed1.61s. Exact
633-filemanifest changed only ledger.py and added the focused test;631outsidefiles unchanged.
Independent spec/quality review approved with no Critical/Important findings. Original v1
bytes, caller replay/fence ordering and original-send capture survive. Task32's reviewed
tagged-subject migration follows; in-memorystrategy experiments are not deploymentqualification
and no actual user database has been migrated.

Original seven-story user journey, philosophical inquiry, actual paired old-queue evaluations,
human-approved environment versions and optional redacted export remain the release target.

Task32 in progress: real temporary empty/populated v1-to-v2 commits, authenticated capture and
checkpoint preservation, rollback stages and FK-on failing controls were exercised. A genuine
rowid/order regression was fixed before the first broad scoped cover. That freeze produced
501 passed / 5 failed / one existing Starlette warning in216.89s. Three legacy synthetic-trigger
fault tests were stopped prematurely by new full schema checks in descriptive/representation
branches; two actual malicious-trigger tests exposed fixed diagnostic/readback compatibility.
Controller clarified structural discrimination versus authority checks without weakening real
dispatch/startup/offline denial or changing those five old test oracles. Corrections are active,
not independently accepted. The new fixture's ignored-DDL-file dependency is also being removed.
No real store, key, provider, native deployment, commit or push has been operated.

The next private provider-worker contract is under independent feasibility review. It is a
bounded authenticated text/catalog transformer, not a staged/qualified provider, gateway issuer
or live connection. Official API source findings are in the separate Claude protocol evidence;
actual source-context/staging, durable qualification and canonical model selection remain open.

Task32 amended freeze passed522tests/oneexistingStarlettewarning in204.25s and parent3high-risk
checks in1.60s. Its source-free authenticated post-migration send/capture/atomicsettlement/replay/
new-node/reverify experiment also passed, with three earlier harness errors separately retained.
Independent review nevertheless rejected acceptance with two reproduced Important findings:
invalid ToolCall result/approval references can evade history preflight, and target v2 growth
can exceed capacity only afterCOMMIT. Reviewer used three owned temporary experiments; no
broad repeat or source edits. R1 is active on the original writer with exact11beforecopies,
639baseline manifest and focused reference/row/byte boundary regressions. No actualuserstoreused.
Provider-worker contract preflight is now ready after five focused amendments, with exact
20-path Task33 plan/queuedbrief retained. No Task33 implementation precedes Task32 acceptance.

Task32 R1 is now accepted: reference regressions12RED+1control→13GREEN; projected-capacity
regressions4RED+2controls→6GREEN; authorizedfive-family279pass/93.57s/no warnings. Parent6R1checks
passed2.05s. Independent scopedrereview marks bothImportantfindings ADDRESSED withno newbreakage.
Fresh full639manifest matches finalR1freeze, exactlythreeR1fileschanged/636unchanged. Sevenplan
stepsclosed; canonicalwhole-storytasksremainopen. Task33 beforemanifest captured from thisaccepted
state and its two beforecopies reverified. Worktree/main HEAD unchanged; no commit or actualdataops.
